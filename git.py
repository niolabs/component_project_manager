class Git(object):

    """ Helper methods to interact with git repositories """

    @staticmethod
    def clone_repo(repo_url, target_dir, branch=None, shallow=False):
        pass

    @staticmethod
    def pull_latest(repo_path):
        pass

    @staticmethod
    def checkout_latest_branch(repo_path, branch_name):
        pass
